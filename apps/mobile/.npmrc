## Prevent npm from hoisting react and react-native to the monorepo root.
## This ensures that apps/mobile/node_modules/react (v19) is always used
## instead of the root node_modules/react (v18) during Metro bundling.
install-strategy=nested
