{
  "name": "@be-candid/extension",
  "version": "1.0.0",
  "private": true,
  "description": "Be Candid Chrome Extension - Awareness Monitor",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf dist be-candid-extension.zip",
    "safari": "npm run build && bash scripts/convert-safari.sh"
  }
}
